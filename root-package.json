{
  "name": "playerguild",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd playerguild && npm start",
    "dev:backend": "node backend/server.js",
    "build": "cd playerguild && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../playerguild && npm install"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3"
  }
}